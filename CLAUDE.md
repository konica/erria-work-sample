## Agent skills

### Issue tracker

GitHub Issues on `konica/erria-work-sample`, via the `gh` CLI. PRs-as-request-surface is off. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical role labels, unchanged: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
