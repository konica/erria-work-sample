import { test } from '../fixtures/test.js';

// docs/qa/uat-test-cases.md — Resolution & outcome (ResolutionSection)
//
// BLOCKED — gap 1: no console navigation path reaches Dai Duong Shipping (see escalation.spec.ts
// for the full reason). Skipped rather than left to fail for a reason unrelated to the Resolution
// & outcome tab itself; un-skip once a navigation path exists.

test.skip('TC-RES-1 — A resolved escalation renders its outcome', () => {
  // BLOCKED — gap 1: no console navigation path reaches Dai Duong Shipping.
});
